{% extends 'base.html.twig' %}

{% block title %}Inscription - SymKitch'Pro{% endblock %}

{% block body %}
<div class=" registerForm">
    <div class="pad">
    </div>
    <div class="col-md-4  justify-content-center shadow-lg bg-dark text-light rounded position-relative">
        <h1 class="mt-0">Inscription</h1>
        <div class="form-control rounded-bottom">

        
            {{ form(form) }}
            <p class="mt-2 text-right">Déjà membre ? <a href="{{ path('app_login') }}">Se connecter</a></p>
        </div>
    </div>
            
</div>    
{% endblock %}

 